<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/alignmentRootLayout"
    android:orientation="vertical"
    android:padding="24dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context=".ui.GoalProfileActivity">

    <!-- 🧭 Main Alignment Status Banner -->
    <TextView
        android:id="@+id/alignmentBanner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="14dp"
        android:text="Analyzing your current alignment..."
        android:textColor="@android:color/black"
        android:textSize="17sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="16dp"
        android:background="@color/alignment_gray" />

    <!-- 🎯 Top Drifted Category -->
    <TextView
        android:id="@+id/categoryDetailText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📌 Top Drifted Goal: --"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:visibility="gone"
        android:layout_marginBottom="8dp" />

    <!-- 📉 Drift Summary Text -->
    <TextView
        android:id="@+id/driftSummaryText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="You're spending more time on escapism apps recently, which may reflect avoidance of key goals like confidence or focus."
        android:textSize="14sp"
        android:textColor="@android:color/black"
        android:visibility="gone"
        android:layout_marginBottom="16dp" />

    <!-- 🧠 Overview Header -->
    <TextView
        android:id="@+id/alignmentHeader"
        android:text="🧠 Alignment Overview"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp" />

    <!-- 📋 Recycler for Insight Entries -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/alignmentRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="12dp"
        android:overScrollMode="always"
        android:contentDescription="Goal alignment insights" />

</LinearLayout>
